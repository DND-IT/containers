---
name: go-runtime
description: Minimal Debian-slim runtime base for statically-compiled Go binaries, no toolchain
version: 0.1.0
platforms:
  - linux/amd64
  - linux/arm64
test: test -f /etc/ssl/certs/ca-certificates.crt && test "$(id -u)" -ne 0
