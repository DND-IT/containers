---
name: node
description: Hardened Debian-slim Node.js base image for building and running Node.js and TypeScript services
version: 0.1.0
platforms:
  - linux/amd64
  - linux/arm64
test: node --version && npm --version && test "$(id -u)" -ne 0
